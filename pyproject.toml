[project]
name = "insecticidas-inventario"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "Flask",
    "Flask-SQLAlchemy",
    "Flask-Login",
    "Werkzeug"
]

[tool.vercel]
entrypoint = "run:app"